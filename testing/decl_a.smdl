#smdl

// Companion module for 'decl_regressions.smdl'. Exports the same name as
// 'decl_b.smdl' so qualified lookups can prove they resolve through the
// correct module.
export const int shadowed = 1;
